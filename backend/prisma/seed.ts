import { PrismaClient, Role } from '@prisma/client'; import bcrypt from 'bcryptjs'; const prisma=new PrismaClient();
async function main(){const passwordHash=await bcrypt.hash('DemoPass123!',12);const admin=await prisma.user.upsert({where:{email:'admin@latticecare.demo'},update:{},create:{email:'admin@latticecare.demo',firstName:'Maya',lastName:'Singh',role:Role.ADMIN,passwordHash}});const patient=await prisma.user.upsert({where:{email:'ananya.shah@demo.test'},update:{},create:{email:'ananya.shah@demo.test',firstName:'Ananya',lastName:'Shah',role:Role.PATIENT,passwordHash}});for(const d of [{email:'meera.iyer@demo.test',first:'Meera',last:'Iyer',specialization:'Internal Medicine',bio:'Preventive care and complex adult health.'},{email:'arjun.menon@demo.test',first:'Arjun',last:'Menon',specialization:'Cardiology',bio:'Evidence-led cardiovascular care.'},{email:'naina.kapoor@demo.test',first:'Naina',last:'Kapoor',specialization:'Dermatology',bio:'Thoughtful skin and hair health care.'}]){const u=await prisma.user.upsert({where:{email:d.email},update:{},create:{email:d.email,firstName:d.first,lastName:d.last,role:Role.DOCTOR,passwordHash}});const doctor=await prisma.doctor.upsert({where:{userId:u.id},update:{},create:{userId:u.id,specialization:d.specialization,bio:d.bio,slotMinutes:30}});for(const weekday of [1,2,3,4,5])await prisma.doctorAvailability.upsert({where:{doctorId_weekday:{doctorId:doctor.id,weekday}},update:{},create:{doctorId:doctor.id,weekday,startTime:'09:00',endTime:'17:00'}})}console.log({admin:admin.email,patient:patient.email,password:'DemoPass123!'})}main().finally(()=>prisma.$disconnect());
